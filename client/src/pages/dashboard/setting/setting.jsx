import { connect } from '../../../store/connect'

function Setting({global,dispatch}) {
    return (
        <div className="content">
            这是登录页面
        </div>
    )
}

export default connect(Setting)