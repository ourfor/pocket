import { MainContainer } from '../../components/layout/layout'

export default function ImportPage({global,dispatch}) {
    return (
        <MainContainer>
            <p>导入课表</p>
        </MainContainer>
    )
}